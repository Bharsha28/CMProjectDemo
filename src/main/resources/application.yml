spring:
  datasource:
    # H2 in-memory database (no MySQL required)
    url: jdbc:h2:mem:cardmaster;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
server:
  port: 8082

# --- Original MySQL config (uncomment and remove H2 block above to use MySQL) ---
# spring:
#   datasource:
#     url: jdbc:mysql://127.0.0.1:3306/db9?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
#     username: root
#     password: Harsha@28
#     driver-class-name: com.mysql.cj.jdbc.Driver
#   jpa:
#     database-platform: org.hibernate.dialect.MySQLDialect
